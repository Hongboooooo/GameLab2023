using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ConstructInputManager : MonoBehaviour
{
    // public event Action BuildEnter, BuildExit;
    // private int build_start = -1;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        // if(Input.GetKeyDown(KeyCode.R))
        // {
        //     Debug.Log("R pressed");
        //     build_start = -1 * build_start;
        //     if(build_start > 0)
        //     {
        //         BuildEnter?.Invoke();
        //     }
        //     else
        //     {
        //         BuildExit?.Invoke();
        //     }
        // }
    }
}
